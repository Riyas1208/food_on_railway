import 'package:flutter/material.dart';

class Railway extends StatelessWidget {
  const Railway({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Railway srrvice Page"),
      ),
    );
  }
}